---
import "../styles/components/frontmatter-chip.css"

interface Props {
    icon: string;
    text: string;
}

const { icon, text} = Astro.props;
---

<div class="frontmatter-chip">
    <img src={`/icons/${icon}`} alt="" />
    <span>{text}</span>
</div>
